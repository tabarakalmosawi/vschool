var caseUniFiction = function (s) {
    //change into arr
    var arr = s.split('');
    var upper = 0;
    var lower = 0;
    if (str[i] == str[i].toUpperCase()) {
        upper++
    } else if (str[i] == str[i].toLowerCase()) {
        lower++
    }
    //check all letters using for loop
    for (var i = 0; i < arr.length; i++) {
        if (lower<upper){
            str[i].toUpperCase
        } else 
    }
    return arr.join("")
}
console.log(caseUniFiction("abaaaC"))


//مو الجواب المطلوووب .. ومابيَّ حيل احط الجواب الصح :)
